mutation CreateDeployment($name: String!, $service: ServiceInput!, $tier: DeploymentTier!, $config: JSON!) {
  createDeployment(
    name: $name
    service: $service
    tier: $tier
    config: $config
  )
}