# Mutation to create a new merkle drop
mutation CreateMerkleDrop(
  $key: String!,
  $network: String!,
  $description: String,
  $contract: Felt!,
  $entrypoint: String!,
  $claims: [MerkleClaimInput!]!
) {
  createMerkleDrop(
    key: $key,
    network: $network,
    description: $description,
    contract: $contract,
    entrypoint: $entrypoint,
    claims: $claims
  ) {
    id
    description
    network
    contract
    entrypoint
    merkleRoot
    createdAt
  }
}
