query PaymasterInfo($name: ID!) {
  paymaster(name: $name) {
    budget
    budgetFeeUnit
    strkFees
    creditFees
    active
    policies {
      totalCount
    }
    team {
      name
    }
  }
} 