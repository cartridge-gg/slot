query DeploymentLogs(
  $project: String!
  $service: DeploymentService!
  $since: Time
  $limit: Int
  $container: String
) {
  deployment(name: $project, service: $service) {
    logs(since: $since, limit: $limit, container: $container) {
      content
      until
    }
  }
}
